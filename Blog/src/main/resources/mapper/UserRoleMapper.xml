<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.dao.UserRoleMapper">

    <!-- 结果映射 -->
    <resultMap id="UserRole" type="com.example.demo.entity.UserRole" autoMapping="true"/>
        <insert id="addUserRole" parameterType="com.example.demo.entity.UserRole">
            insert into user_role(rid, uid) VALUES (#{rid},#{uid})
        </insert>

</mapper>
